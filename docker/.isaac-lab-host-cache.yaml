# Generated by docker/container.py. Do not commit.
services:
  isaac-lab-base:
    volumes:
      - type: bind
        source: ${HOST_ISAACSIM_KIT_CACHE_PATH}
        target: ${DOCKER_ISAACSIM_ROOT_PATH}/kit/cache
      - type: bind
        source: ${HOST_OMNIVERSE_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.cache/ov
      - type: bind
        source: ${HOST_NVIDIA_GL_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.cache/nvidia/GLCache
      - type: bind
        source: ${HOST_NVIDIA_COMPUTE_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.nv/ComputeCache

  isaac-lab-ros2:
    volumes:
      - type: bind
        source: ${HOST_ISAACSIM_KIT_CACHE_PATH}
        target: ${DOCKER_ISAACSIM_ROOT_PATH}/kit/cache
      - type: bind
        source: ${HOST_OMNIVERSE_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.cache/ov
      - type: bind
        source: ${HOST_NVIDIA_GL_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.cache/nvidia/GLCache
      - type: bind
        source: ${HOST_NVIDIA_COMPUTE_CACHE_PATH}
        target: ${DOCKER_USER_HOME}/.nv/ComputeCache
