Changed
^^^^^^^

* Pinned the experimental Newton fork to validated dense FeatherPGS response optimizations and an opt-in
  ``FEATHER_PGS_DENSE_ROW_BUDGETS=1`` layout. The layout remained disabled by default and required task- and
  hardware-specific benchmarking before use.
